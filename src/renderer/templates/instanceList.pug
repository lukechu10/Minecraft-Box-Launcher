.ui.raised.segment(style="margin-top: 10px;")
	.ui.divided.items
		//- structure: InstanceSave[]
		- if(data == undefined || data.length == 0)
			p= "You don't have any instances yet. Create one to start playing. 😆"
		- else
			//- - for(let i = 0; i < data.length; i++)
